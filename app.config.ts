export default defineAppConfig({
  ui: {
    button: {
      slots: {
        base: 'font-bold'
      }
    },
    colors: {
      primary : 'var(--ui-primary)',
      accent : 'var(--ui-accent)',
      bright : 'var(--ui-bright)',
      white : 'var(--ui-white)',
      dark : 'var(--ui-dark)',
    },
  }
})

